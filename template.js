const firstName = 'Tafsirul';
const lastName = 'Alam';
const fullName = firstName + " " + lastName;
const fullName2 = `${firstName} ${lastName}`;
console.log(fullName2);

const multiLine = `prothom line
ditio line
tritio line`;
console.log(multiLine);